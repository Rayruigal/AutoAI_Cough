use_tfkeras = True

# True => tensorflow.keras. It works with tensorflow >= 1.14.0
# False => keras:  It works with tensorflow <= 1.15.0 and keras==2.2.4 installed
# Note: autokeras requires tensorflow >= 2.3.0
